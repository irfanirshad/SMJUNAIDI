<svg width="1024" height="1024" viewBox="0 0 1024 1024" xmlns="http://www.w3.org/2000/svg">
  <!-- Note: This is the foreground layer for Android adaptive icon -->
  <!-- Background color (#29beb3) is set in app.json -->
  
  <!-- Safe zone circle guide (66% of canvas) -->
  <!-- <circle cx="512" cy="512" r="338" fill="none" stroke="#ff0000" stroke-width="2" stroke-dasharray="10,5" opacity="0.3"/> -->
  
  <!-- White circle background for logo (within safe zone) -->
  <circle cx="512" cy="512" r="300" fill="#ffffff"/>
  
  <!-- Logo Placeholder - Replace with your smallLogo.png -->
  <!-- Scaled to fit within the safe zone -->
  <g transform="translate(512, 512) scale(1.4)">
    <!-- This is a placeholder representation -->
    <!-- Replace this with your actual smallLogo.png in a design tool -->
    <rect x="-110" y="-118" width="220" height="236" fill="#29beb3" rx="20"/>
    <text x="0" y="50" font-family="Arial, Helvetica, sans-serif" font-size="140" font-weight="bold" fill="#ffffff" text-anchor="middle">B</text>
  </g>
</svg>
