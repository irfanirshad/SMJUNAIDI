<svg width="1242" height="2436" viewBox="0 0 1242 2436" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
  <!-- Background -->
  <rect width="1242" height="2436" fill="#29beb3"/>
  
  <!-- Main Content Group - Centered -->
  <g transform="translate(621, 1218)">
    <!-- Logo Placeholder -->
    <!-- Replace this with your smallLogo.png as an embedded image -->
    <!-- For now, showing a simplified logo representation -->
    <g transform="translate(0, -120)">
      <!-- Logo Background Circle -->
      <circle r="80" fill="#ffffff" opacity="0.15"/>
      
      <!-- Simple Logo Representation (replace with actual image) -->
      <!-- This is a placeholder - embed your smallLogo.png here -->
      <rect x="-50" y="-50" width="100" height="100" fill="#ffffff" rx="15" opacity="0.9"/>
      <text x="0" y="15" font-family="Arial, Helvetica, sans-serif" font-size="48" font-weight="bold" fill="#29beb3" text-anchor="middle">B</text>
    </g>
    
    <!-- App Name -->
    <text x="0" y="80" font-family="Arial, Helvetica, sans-serif" font-size="80" font-weight="bold" fill="#ffffff" text-anchor="middle">
      Babymart
    </text>
    
    <!-- Tagline -->
    <text x="0" y="130" font-family="Arial, Helvetica, sans-serif" font-size="24" fill="#ffffff" opacity="0.9" text-anchor="middle">
      Everything for Your Little One
    </text>
    
    <!-- Decorative Elements -->
    <circle cx="-250" cy="-200" r="8" fill="#ffffff" opacity="0.4"/>
    <circle cx="250" cy="-180" r="6" fill="#ffffff" opacity="0.4"/>
    <circle cx="-280" cy="250" r="10" fill="#ffd700" opacity="0.3"/>
    <circle cx="270" cy="230" r="7" fill="#ff69b4" opacity="0.3"/>
    <circle cx="-200" cy="0" r="5" fill="#ffffff" opacity="0.3"/>
    <circle cx="220" cy="20" r="6" fill="#ffffff" opacity="0.3"/>
  </g>
  
  <!-- Note: To use your actual smallLogo.png, you can:
       1. Convert the PNG to base64 and embed it as: <image href="data:image/png;base64,..." />
       2. Or use this SVG template and replace with PNG in your design tool (Figma/Sketch)
       3. Or use the logo directly in React Native splash configuration
  -->
</svg>
